- [Production DB access](prod-db-access.md) — PROD_DATABASE_URL is empty in dev shell (production-only env var); PG* vars point to a different Neon DB; must temporarily copy URL into dev env to reach real production DB.
